// lib/features/intro/splash_intro_config.dart
import 'package:flutter/material.dart';

const Duration splashTimelineDuration = Duration(milliseconds: 5000);
const Duration splashPostDelay = Duration(milliseconds: 1500);

// Timeline intervals (0–1)
const double splashLogoIntervalBegin = 0.0;
const double splashLogoIntervalEnd = 0.40;

const double splashSwapIntervalBegin = 0.40;
const double splashSwapIntervalEnd = 0.70;

const double splashHoldEnd = 1.0;

// Sizes
const double splashLogoSize = 140.0;
const double splashMainWidth = 1120.0;
const double splashMainHeight = 328.0;

// Palette
const Color splashBackgroundColor = Color(0xFF0F172A);
